# Add .deno/bin to PATH for this project
PATH_add .deno/bin
# PATH_add ~/.local/bin

# Get the latest Claude version dynamically
# LATEST_CLAUDE=$(find ~/.local/share/claude/versions/ -type f -executable | sort -V | tail -1)
# alias cld="$LATEST_CLAUDE --dangerously-skip-permissions --settings .claude/settings-auto.json"
