# Frontmatter-to-Schema Environment Configuration
# Copy this file to .env and customize as needed

# Installation Configuration
# Custom installation directory (default: ~/.deno)
# FRONTMATTER_TO_SCHEMA_INSTALL_ROOT=/custom/path/.deno

# Binary name (default: frontmatter-to-schema)
# FRONTMATTER_TO_SCHEMA_BINARY_NAME=frontmatter-to-schema

# Git branch to install from (default: main)
# FRONTMATTER_TO_SCHEMA_BRANCH=main

# Deno permissions (default: --allow-read --allow-write --allow-env --allow-run)
# FRONTMATTER_TO_SCHEMA_PERMISSIONS=--allow-read --allow-write --allow-env --allow-run

# Runtime Configuration
# Enable test mode (uses mock analyzer instead of Claude)
# FRONTMATTER_TO_SCHEMA_TEST_MODE=false

# Enable debug mode
# FRONTMATTER_TO_SCHEMA_DEBUG=false

# API Tokens (for future use when Claude API is integrated)
# FRONTMATTER_TO_SCHEMA_CLAUDE_API_KEY=your-claude-api-key-here

# GitHub Token (for private repository access or rate limit bypass)
# FRONTMATTER_TO_SCHEMA_GITHUB_TOKEN=your-github-token-here