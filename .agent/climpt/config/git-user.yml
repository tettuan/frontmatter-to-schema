# Breakdown Configuration
params:
  two:
    directiveType:
      pattern: "^(find-oldest|merge-up|group-commit|list-select|decide-branch|merge-cleanup)$"
    layerType:
      pattern: "^(descendant-branch|base-branch|unstaged-changes|pr-branch|working-branch|develop-branches)$"
