# Breakdown Configuration
params:
  two:
    directiveType:
      pattern: "^(analyze-deep|trace-error|plan-logger|design-flow)$"
    layerType:
      pattern: "^(project-issues|cause-analysis|debug-strategy|reproducible-steps)$"
