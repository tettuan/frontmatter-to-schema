Map the extracted data to the template structure.

Extracted Data:
{{EXTRACTED_DATA}}

Schema:
{{SCHEMA}}

Instructions:
1. Take the extracted data and transform it to match the template structure
2. Replace template placeholders (e.g., {{fieldName}}) with corresponding data values
3. Return ONLY a valid JSON object with the mapped result
4. The result should be ready to use as the final output
5. Use empty string for missing optional fields
6. Use the field value directly without extra quotes

Return your response as a single JSON object with no additional text or explanation.
